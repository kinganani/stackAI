services:
  db:
    image: postgres:16
    environment:
      POSTGRES_DB: fraislink
      POSTGRES_USER: fraislink
      POSTGRES_PASSWORD: fraislink
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
